#include "wire.h"

int main(int argc, char *argv[])
{
    std::string from_wire;
    while (1)
    {
        std::cout << "Enter a value: ";
        std::string from_stdin;
        std::getline(std::cin,from_stdin);
    }
}